.clearfix
  = f.label :name, 'Tytuł', :class => :label
  .input
    = f.text_field :name, :class => 'text_field'
.clearfix
  = f.label :body, 'Treść', :class => :label
  .input
    = f.text_area :body, {:class => "tinymce text_area", :rows => 40, :cols => 160}
    = tinymce
.clearfix
  = f.label :user_id, t("activerecord.attributes.post.user_id", :default => "User"), :class => :label


.form-actions
  %button{:class => "btn primary", :type => "submit"} Save
  or
  = link_to "Cancel", tournament_posts_path(@tournament)
